name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "21"
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install

      - name: Run tests with coverage
        run: yarn test:coverage

      - name: Generate coverage badge
        uses: tj-actions/coverage-badge-js@v2
        if: github.ref == 'refs/heads/main'
        with:
          output: coverage/coverage-badge.svg

      - name: Verify coverage files
        run: |
          echo "Generated files:"
          ls -la coverage/
          cat coverage/coverage-summary.json

      - name: Commit coverage badge and lockfile
        if: github.ref == 'refs/heads/main'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add coverage/coverage-badge.svg yarn.lock
          git diff --staged --quiet || git commit -m "Update coverage badge and lockfile"
          git push