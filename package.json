{
  "name": "figma-pr-sync",
  "version": "1.0.0",
  "description": "GitHub Action to sync Figma designs in PR descriptions",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=21"
  }
}